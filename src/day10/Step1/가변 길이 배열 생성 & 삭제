라이브러리 함수 사용하지 않고 가변 길이 배열 생성 & 삭제 

무한 배열 등록
1. count 1 증가시키기
2. 새로운 배열 생성 (증가시킨 count 만큼의)
3. 반복문을 기존 배열 길이만큼 돌려서 새로운 배열에 대입
4. 새로운 배열 길이 -1의 인덱스에 새롭게 받은 값을 대입
5. 새로운 배열을 기존 배열에 대입

무한 배열 삭제
1. 삭제할 코드 or 번호 받기
2. 반복문을 기존 배열의 길이만큼 돌려서 삭제할 코드 or 번호 겹치는 부분 찾고
3. 반복문 안에 반복문으로 삭제할 코드 or 번호 겹치는 부분부터 기존 배열의 길이 -1만큼 돌려서 기존 배열의 인덱스에 한칸 뒤에 인덱스의 값을 대입
4. count 1 감소
5. 새로운 배열 생성 (감소시킨 count 만큼의)
6. 반복문을 새로운 배열의 길이만큼 돌려서 기존 배열의 값을 새로운 배열에 대입
7. 새로운 배열을 다시 기존 배열에 대입